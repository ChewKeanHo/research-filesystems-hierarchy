# `/root/Desktop`

[![banner](/.internals/trademarks/banner_1200x100.svg)](#)

This directory houses the `root`'s files displayable onto the Desktop (screen).
Hidden files by default are hidden unless the `root` configures it to be shown
explictly.

The directory's access is **RESTRICTED TO OWNER** by default.
